# Google Maps Platform — enable Geocoding API and Street View Static API (metadata is free)
GOOGLE_MAPS_API_KEY=

# OpenAI — used by verification.PropertyVerifier (vision + JSON). Optional until step 8 runs.
OPENAI_API_KEY=
